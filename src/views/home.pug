extends base
  block content
    h1 Masked Wolf Playlist
    div#playlist
      each track in tracks
        div
          h3= track.name
          p Play count: #{track.playcount}
          p Listeners: #{track.listeners}
